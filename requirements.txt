# Versions below were tested end-to-end. Pinning them means the live
# Streamlit Cloud deployment behaves exactly like your local machine,
# and the saved model.pkl loads without scikit-learn version warnings.
streamlit==1.57.0
scikit-learn==1.8.0
pandas==3.0.2
numpy==2.4.4
joblib==1.5.3
shap==0.51.0
matplotlib==3.10.8
