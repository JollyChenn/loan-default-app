# ---- Raw dataset files: too big for GitHub, download from Kaggle instead ----
cs-training.csv
cs-test.csv
credit_risk_dataset.csv

# ---- NOTE: model.pkl is intentionally NOT ignored ----
# The deployed Streamlit Cloud app needs model.pkl in the repo to work.
# We keep it small by capping tree depth in train.py so it commits fine.

# ---- Python junk ----
__pycache__/
*.pyc
.venv/
venv/
.ipynb_checkpoints/

# NOTE: confusion_matrix.png and shap_importance.png ARE committed
# so they show up in the deployed app's "Model Insights" tab.
